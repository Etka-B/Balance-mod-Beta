namespace = flavor_sweden
flavor_sweden.1 = {
	type = country_event
	title = flavor_sweden.1.title
	desc = flavor_sweden.1.desc
	outcome = neutral
	historical_info = flavor_sweden.1.historical_info

	fire_only_once = yes
	dynamic_historical_event = {
		tag = SWE
		from = 1337.1.1
		to = 1470.1.1
		monthly_chance = 10
	}

	trigger = {
		religion = religion:catholic
		owns = location:stockholm
		at_war = no
		is_subject = no
		has_regent = no
	}


	illustration_tags = {
		10 = angry
		10 = exterior
	}

	immediate = {
		event_illustration_estate_effect = { foreground = estate_type:clergy_estate background = estate_type:clergy_estate }
		create_named_dynasty = lodbrok_dynasty

		create_character = {
			first_name = name_ragnar
			adm = { 80 100 }
			dip = { 80 100 }
			mil = { 80 100 }
			birth_date = 1320.1.1
			culture = culture:swedish
			religion = religion:norse
			dynasty = dynasty:lodbrok_dynasty
			estate = estate_type:nobles_estate
			create_in_limbo = yes
			save_scope_as = ragnar_lodbrok
			script = swe_ragnar_lodbrok
		}
	}


	option = {
        historical_option = yes
        name = flavor_sweden.1.a
        add_stability = stability_severe_bonus
        add_legitimacy = legitimacy_mild_bonus
	    add_country_modifier = { 
        modifier = minor_massive_bonus 
        years = 50 
        mode = add_and_extend 
        }
    }
	option = {

        name = flavor_sweden.1.b
		set_new_ruler = scope:ragnar_lodbrok
		add_stability = -60
        add_legitimacy = -30

		change_religion = religion:norse
		if = {
			limit = {
				has_ruler = yes
				ruler = { has_character_modifier = enacted_placitum_regium }
			}

			ruler = { remove_character_modifier = enacted_placitum_regium }
		}
		change_religion_for_ruler_and_family = { country = ROOT religion = religion:norse }
		# Balance mod: this option previously had no compensating upside for the harsh
		# stability/legitimacy hit - give it a 50-year bonus mirroring option A's
		# minor_massive_bonus, themed around the old faith being tolerated again.
		add_country_modifier = {
			modifier = flavor_sweden_valhalla_bonus
			years = 50
			mode = add_and_extend
		}
	}
}