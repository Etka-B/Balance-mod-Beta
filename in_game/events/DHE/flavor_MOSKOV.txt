namespace = flavor_muscovy

flavor_muscovy.1 = { 
    type = country_event
    
    title = flavor_muscovy.1.title
    desc = flavor_muscovy.1.desc
    outcome = neutral
    fire_only_once = yes

    dynamic_historical_event = {
        tag = MOS
        
        from = 1337.1.1
        to = 1350.1.1        
        monthly_chance = 100
    }

    trigger = {
        is_human = yes 
    }

    option = { 
        name = flavor_muscovy.1.a
        historical_option = yes


        add_country_modifier = { 
            modifier = muscovy_massive_bonus 
            years = 50 
            mode = add_and_extend 
        }

        ai_will_select = {
            value = 90
        }
    }
}