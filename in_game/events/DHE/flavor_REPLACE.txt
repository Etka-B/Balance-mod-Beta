flavor_ver.4 = {
	type = country_event
	title = flavor_ver.4.title
	desc = flavor_ver.4.desc
	outcome = positive

	fire_only_once = yes
	dynamic_historical_event = {
		tag = FLO
		from = 1337.1.1
		to = 1360.1.1
		monthly_chance = 100
	}

	trigger = {
		in_scaligeri_war_trigger = yes
		country_exists = cLUC
		root_or_scaligeri_war_participant_controls = { location = locationlucca }
		scopetarget_war = {
			is_a_defender = cLUC
		}
		
	}

	immediate = {
		set_up_della_scala_targets = yes

		event_illustration_estate_effect = {
			background = estate_typeburghers_estate
			foreground = estate_typenobles_estate
		}
		scopetarget_war.attacker_leader = {
			save_scope_as = war_leader
		}
		locationpescia = {
			save_scope_as = target_location
		}
		cLUC = {
			save_scope_as = target_country
		}
		save_scope_as = from_country
		peace_treatyver_florentine_demands = {
			save_scope_as = target_peace_treaty
		}
	}
	illustration_tags = {
		10 = armed
		10 = exterior
	}

	option = {
		name = flavor_ver.4.a

		historical_option = yes
		
		ai_will_select  = { value = 50 }

		scaligeri_war_peace_treaty_choice = {
			country = FLO
			flag = pescia_demand
			antagonism = low
		}
	}

	option = {
		name = flavor_ver.4.b

		ai_will_select  = { value = 50 }
		
		scaligeri_war_peace_treaty_choice = {
			country = FLO
			flag = lucca_demand
			antagonism = high
		}
	}

	after = {
		scopewar_leader = {
			trigger_event_non_silently = {
				id = flavor_ver.6
			}
		}
	}
}